export default function AlternanceV2Layout({ children }: { children: React.ReactNode }) {
  return (
    <div className="alternance-module crm-v2" style={{ minHeight: '100%' }}>
      {children}
    </div>
  )
}
